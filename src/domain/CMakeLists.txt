# Domain model lives here from Phase 2 onward (Model, BeadField,
# Selection, ColorTable, BeadSymbols, coordinate calculators, ...).
# Empty for now — leave as a placeholder so the directory is part of
# the configured tree.
